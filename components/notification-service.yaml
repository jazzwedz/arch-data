id: notification-service
name: Notification Service
type: microservice
status: production
owner: platform-team
tags:
  - platform
  - messaging
  - email
  - sms
  - push
description:
  oneliner: Centralized system for sending notifications across all channels
  technical: Node.js microservice with template engine (Handlebars) and channel routing. Supports email (SendGrid), SMS (Twilio), push notifications (Firebase), in-app, and webhook. Priority queue with rate limiting per channel. Retry mechanism with exponential backoff. Delivery tracking and bounce handling.
  business: Ensures reliable delivery of transactional and marketing messages to customers. Unifies communication channels and enables business teams to manage templates without developer intervention.
  description: |-
    Node.js microservice with template engine (Handlebars) and channel routing. Supports email (SendGrid), SMS (Twilio), push notifications (Firebase), in-app, and webhook. Priority queue with rate limiting per channel. Retry mechanism with exponential backoff. Delivery tracking and bounce handling.

    Ensures reliable delivery of transactional and marketing messages to customers. Unifies communication channels and enables business teams to manage templates without developer intervention.
risks:
  - Spam protection - misconfiguration can cause mass sending
  - Email deliverability depends on domain reputation and SPF/DKIM configuration
  - GDPR - must respect customer opt-out preferences
diagram:
  color: '#EC4899'
  shape: rectangle
links:
  - target: event-bus
    role: calls
    protocol: async
    description: Pocuva domenove eventy a triggeruje prislusne notifikacie
  - target: sendgrid-api
    role: calls
    protocol: rest
    description: SendGrid API pre odosielanie emailov
  - target: redis-cache
    role: calls
    protocol: db
    description: Template cache and delivery deduplication
  - target: identity-service
    role: serves
    protocol: async
    description: Triggers notifications for login alerts and password resets
  - target: order-service
    role: serves
    protocol: async
    description: Triggers order confirmation and status update notifications
  - target: notification-db
    role: calls
    protocol: db
    description: Depends on
  - target: eod-reconciliation
    role: calls
    protocol: rest
    description: Receives break alert triggers from reconciliation
  - target: aml-screening-job
    role: serves
    protocol: rest
    description: Sends urgent match notifications
  - target: eod-reconciliation
    role: serves
    protocol: rest
    description: Sends alerts on critical breaks
schema_version: 2
